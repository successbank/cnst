# 카카오톡 알림 & 중개판매 기능 테스트 가이드

> 최종 업데이트: 2026-03-11
> 대상 수정: 재발송 세션 버그, 재발송 횟수 제한, 일일 발송 제한, 이력 조회 CSRF 보호

---

## 목차

1. [사전 확인](#1-사전-확인)
2. [테스트 환경 설정](#2-테스트-환경-설정)
3. [테스트 시나리오 A: 판매의뢰 승인 + 카카오 발송](#3-테스트-시나리오-a)
4. [테스트 시나리오 B: 판매의뢰 거절 + 카카오 발송](#4-테스트-시나리오-b)
5. [테스트 시나리오 C: 재발송 + 횟수 제한](#5-테스트-시나리오-c)
6. [테스트 시나리오 D: 일일 발송 제한](#6-테스트-시나리오-d)
7. [테스트 시나리오 E: 발송 이력 조회 CSRF](#7-테스트-시나리오-e)
8. [DB 직접 확인 명령어](#8-db-직접-확인-명령어)
9. [테스트 데이터 정리](#9-테스트-데이터-정리)
10. [트러블슈팅](#10-트러블슈팅)

---

## 1. 사전 확인

### 현재 환경 상태

| 항목 | 현재 값 | 설명 |
|------|---------|------|
| 카카오 테스트 모드 | **ON** (`kakao_test_mode=1`) | 실제 API 호출 안 함, DB만 기록 |
| 알림톡 활성화 | ON (`kakao_notification_enabled=1`) | |
| 일일 발송 제한 | 1000건 (`kakao_daily_limit=1000`) | |
| 재시도 횟수 | 3회 (`kakao_retry_count=3`) | |
| 관리자 계정 | `admin` (2FA 비활성) | |
| 테스트 가능 게시글 | #6918 (pending), #6917 (pending) | |

### 접속 URL

| 페이지 | URL |
|--------|-----|
| 관리자 로그인 | https://cnst.co.kr/admin/admin_login.php |
| 위탁판매 관리 목록 | https://cnst.co.kr/admin/admin_consignment.php |
| 위탁판매 상세 (예시) | https://cnst.co.kr/admin/admin_consignment_view.php?id=6918 |
| 카카오 설정 관리 | https://cnst.co.kr/admin/admin_kakao.php |

---

## 2. 테스트 환경 설정

### 2-1. 테스트 모드 확인 (필수)

**테스트 모드가 켜져 있으면 실제 카카오 API를 호출하지 않습니다.** DB 기록만 남깁니다.

```bash
# 현재 설정 확인
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT setting_key, setting_value FROM site_settings WHERE setting_group='kakao' AND setting_key IN ('kakao_test_mode','kakao_notification_enabled','kakao_daily_limit','kakao_retry_count');"
```

또는 **관리자 페이지**에서: `관리자 로그인 → 카카오 설정` 페이지에서 확인 가능

### 2-2. 테스트용 일일 제한 낮추기 (시나리오 D용)

```bash
# 일일 제한을 2건으로 낮춤 (테스트 후 반드시 원복!)
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "UPDATE site_settings SET setting_value='2' WHERE setting_key='kakao_daily_limit';"
```

### 2-3. 테스트용 재시도 제한 낮추기 (시나리오 C용)

```bash
# 재시도 제한을 1회로 낮춤 (테스트 후 반드시 원복!)
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "UPDATE site_settings SET setting_value='1' WHERE setting_key='kakao_retry_count';"
```

---

## 3. 테스트 시나리오 A: 판매의뢰 승인 + 카카오 발송 {#3-테스트-시나리오-a}

### 목적
- 승인 처리 시 카카오 알림톡이 정상 기록되는지 확인
- `sent_by`에 관리자 ID가 올바르게 저장되는지 확인

### 단계

1. **관리자 로그인**
   - https://cnst.co.kr/admin/admin_login.php
   - 계정: `admin` / (비밀번호)

2. **위탁판매 목록으로 이동**
   - https://cnst.co.kr/admin/admin_consignment.php
   - `대기중` 상태 필터 클릭

3. **상세 페이지 진입**
   - 게시글 #6918 클릭 → https://cnst.co.kr/admin/admin_consignment_view.php?id=6918

4. **승인 처리**
   - [승인] 버튼 클릭 → 모달 팝업
   - 중개 금액: `1,500,000` 입력
   - 추가 메시지: `테스트 승인 메시지입니다` (선택)
   - [미리보기] 버튼 클릭 → 알림톡 미리보기 확인
   - [승인 및 알림 발송] 클릭

5. **결과 확인**
   - 성공 알림 표시 (테스트 모드 안내 포함)
   - 페이지 새로고침 후:
     - 상태: `승인(게시중)`으로 변경
     - 알림톡: `발송완료` 배지 표시
     - 하단 **카카오 알림톡 발송 이력** 섹션에 기록 표시

### DB 검증

```bash
# 발송 기록 확인 - sent_by가 'admin'이어야 함
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT id, message_type, status, sent_by, parent_notification_id, created_at
      FROM kakao_notifications
      WHERE board_type='consignment' AND board_id=6918
      ORDER BY id DESC;"

# board_consignment 상태 확인
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT id, status, kakao_send_status, kakao_sent_at, reviewed_by
      FROM board_consignment WHERE id=6918;"
```

### 예상 결과

| 항목 | 기대값 |
|------|--------|
| kakao_notifications.status | `sent` |
| kakao_notifications.sent_by | `admin` (NULL이 아님) |
| kakao_notifications.message_type | `CONSIGN_APPROVED` |
| kakao_notifications.parent_notification_id | `NULL` (최초 발송) |
| board_consignment.status | `approved` |
| board_consignment.kakao_send_status | `sent` |

---

## 4. 테스트 시나리오 B: 판매의뢰 거절 + 카카오 발송 {#4-테스트-시나리오-b}

### 목적
- 거절 처리 시 카카오 알림톡이 정상 기록되는지 확인

### 단계

1. 관리자 로그인 상태에서 게시글 #6917 상세 페이지 진입
2. [거절] 버튼 클릭 → 모달 팝업
3. 거절 사유: `테스트 거절 사유입니다` 입력
4. [미리보기] → 내용 확인
5. [거절 확정 및 알림 발송] 클릭

### DB 검증

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT id, message_type, status, sent_by, created_at
      FROM kakao_notifications
      WHERE board_type='consignment' AND board_id=6917
      ORDER BY id DESC;"
```

### 예상 결과

| 항목 | 기대값 |
|------|--------|
| message_type | `CONSIGN_REJECTED` |
| status | `sent` |
| sent_by | `admin` |
| board_consignment.status | `rejected` |

---

## 5. 테스트 시나리오 C: 재발송 + 횟수 제한 {#5-테스트-시나리오-c}

### 목적
- 재발송 시 `sent_by`가 정상 기록되는지 (기존 버그: NULL)
- `parent_notification_id`가 설정되는지
- 횟수 제한이 동작하는지

### 사전 조건
- 시나리오 A 완료 (게시글 #6918이 approved 상태)

### 단계

#### 단계 1: 첫 번째 재발송

1. 게시글 #6918 상세 페이지
2. 알림톡 섹션의 [재발송] 버튼 클릭 (또는 발송 이력 테이블의 재발송 버튼)
3. 확인 대화상자에서 [확인]
4. 성공 메시지 확인

#### 단계 2: DB 확인

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT id, message_type, status, sent_by, parent_notification_id, created_at
      FROM kakao_notifications
      WHERE board_type='consignment' AND board_id=6918
      ORDER BY id DESC;"
```

**확인 포인트:**
- 재발송 레코드의 `sent_by` = `admin` (NULL이 아님 ← 버그 수정 확인)
- 재발송 레코드의 `parent_notification_id` = 원본 알림 ID (NULL이 아님 ← 추적 기능 확인)

#### 단계 3: 횟수 제한 테스트 (빠른 테스트)

```bash
# 재시도 제한을 1회로 낮춤
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "UPDATE site_settings SET setting_value='1' WHERE setting_key='kakao_retry_count';"

# PHP config 반영을 위해 PHP-FPM 재시작 (opcache 초기화)
cd /home/cnst/www/html/webservice && docker compose restart php
```

5. 페이지 새로고침 후 다시 [재발송] 클릭
6. **"재발송 횟수 제한(1회)을 초과했습니다."** 에러 메시지 확인

#### 단계 4: 원복

```bash
# 재시도 제한 원복
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "UPDATE site_settings SET setting_value='3' WHERE setting_key='kakao_retry_count';"
```

---

## 6. 테스트 시나리오 D: 일일 발송 제한 {#6-테스트-시나리오-d}

### 목적
- `KAKAO_DAILY_LIMIT` 초과 시 발송이 차단되는지 확인

### 단계

#### 단계 1: 일일 제한을 현재 발송 건수 이하로 설정

```bash
# 오늘 발송 건수 확인
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT COUNT(*) as today_count FROM kakao_notifications
      WHERE DATE(created_at) = CURDATE() AND status != 'failed';"

# 일일 제한을 0으로 설정 (모든 발송 차단)
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "UPDATE site_settings SET setting_value='0' WHERE setting_key='kakao_daily_limit';"
```

> **주의**: `KAKAO_DAILY_LIMIT`이 0이면 제한 체크를 건너뛰도록 구현되어 있으므로, 제한을 테스트하려면 1 이상으로 설정해야 합니다.

```bash
# 일일 제한을 1로 설정 (이미 1건 이상 발송했다면 차단됨)
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "UPDATE site_settings SET setting_value='1' WHERE setting_key='kakao_daily_limit';"

# PHP 설정 반영
cd /home/cnst/www/html/webservice && docker compose restart php
```

#### 단계 2: 발송 시도

1. 아직 pending인 게시글이 있다면 승인 시도
2. 또는 이미 approved/rejected인 게시글에서 재발송 시도
3. **"일일 발송 한도(1건)를 초과했습니다."** 에러 확인

#### 단계 3: 원복 (필수!)

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "UPDATE site_settings SET setting_value='1000' WHERE setting_key='kakao_daily_limit';"
```

---

## 7. 테스트 시나리오 E: 발송 이력 조회 CSRF 보호 {#7-테스트-시나리오-e}

### 목적
- GET → POST 전환이 정상 동작하는지
- CSRF 토큰 없이 직접 요청 시 차단되는지

### 단계 1: 정상 동작 확인

1. 게시글 #6918 (승인 상태) 상세 페이지 진입
2. 페이지 하단 **카카오 알림톡 발송 이력** 섹션이 정상 로드되는지 확인
3. 발송 이력 테이블에 데이터가 표시되어야 함

### 단계 2: CSRF 차단 확인 (브라우저 개발자 도구)

1. 브라우저에서 **F12** → **Console** 탭 열기
2. 다음 JavaScript를 실행:

```javascript
// CSRF 토큰 없이 POST 요청 → 403 반환 확인
fetch('/ajax/get_consignment_kakao_history.php', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: 'id=6918'
})
.then(r => r.json())
.then(data => console.log('CSRF 없는 요청 결과:', data))
.catch(e => console.error('에러:', e));
```

3. 결과: `{"success":false,"message":"CSRF 토큰이 유효하지 않습니다."}` 확인

```javascript
// 기존 GET 방식 → 405 반환 확인
fetch('/ajax/get_consignment_kakao_history.php?id=6918')
.then(r => r.json())
.then(data => console.log('GET 요청 결과:', data))
.catch(e => console.error('에러:', e));
```

4. 결과: `{"success":false,"message":"POST 요청만 허용됩니다."}` 확인

### 예상 결과

| 요청 방식 | CSRF 토큰 | 결과 |
|-----------|-----------|------|
| POST + CSRF (정상) | 있음 | 200 + 이력 데이터 |
| POST (CSRF 없음) | 없음 | 403 + CSRF 에러 |
| GET | 상관없음 | 405 + 메서드 에러 |

---

## 8. DB 직접 확인 명령어

### 전체 발송 이력 조회

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT id, board_id, message_type, status, sent_by,
             parent_notification_id, error_message, created_at
      FROM kakao_notifications
      ORDER BY id DESC LIMIT 20;"
```

### 오늘 발송 통계

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT
        COUNT(*) as total,
        SUM(status='sent') as sent,
        SUM(status='failed') as failed,
        SUM(status='pending') as pending,
        SUM(parent_notification_id IS NOT NULL) as resends
      FROM kakao_notifications
      WHERE DATE(created_at) = CURDATE();"
```

### 재발송 추적 (parent 체인)

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT id, board_id, status, sent_by, parent_notification_id, created_at
      FROM kakao_notifications
      WHERE board_type='consignment' AND board_id=6918
      ORDER BY id;"
```

### 위탁판매 게시글 상태 확인

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT id, title, status, kakao_send_status, kakao_sent_at, reviewed_by, reviewed_at
      FROM board_consignment
      WHERE id IN (6917, 6918);"
```

### PHP 에러 로그 확인

```bash
# PHP-FPM 에러 로그
docker logs project1_php --tail 30

# Nginx 에러 로그
docker logs project1_web --tail 30

# WAF 로그
cat /home/cnst/www/html/webservice/html/html/logs/waf.log | tail -20
```

---

## 9. 테스트 데이터 정리

### 테스트 후 발송 이력 삭제

```bash
# 테스트 발송 기록만 삭제 (board_id 기준)
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "DELETE FROM kakao_notifications
      WHERE board_type='consignment' AND board_id IN (6917, 6918);"
```

### 게시글 상태 원복

```bash
# pending 상태로 원복
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "UPDATE board_consignment
      SET status='pending',
          kakao_send_status=NULL, kakao_sent_at=NULL,
          reviewed_by=NULL, reviewed_at=NULL,
          approved_price=NULL, approved_message=NULL,
          reject_reason=NULL
      WHERE id IN (6917, 6918);"
```

### 설정 원복 (테스트 중 변경했다면)

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db -e "
  UPDATE site_settings SET setting_value='1000' WHERE setting_key='kakao_daily_limit';
  UPDATE site_settings SET setting_value='3' WHERE setting_key='kakao_retry_count';
  UPDATE site_settings SET setting_value='1' WHERE setting_key='kakao_test_mode';
"
```

---

## 10. 트러블슈팅

### 문제: 페이지 접속 시 빈 화면 / 500 에러

```bash
# PHP 에러 로그 확인
docker logs project1_php --tail 50 2>&1 | grep -i error

# Nginx 에러 로그 확인
docker logs project1_web --tail 50 2>&1 | grep -i error
```

### 문제: CSRF 토큰 에러

- 세션이 만료되었을 수 있음 → 관리자 로그인 다시 수행
- 관리자 세션 타임아웃: 1시간 (비활동 시)

### 문제: 설정 변경이 반영 안 됨

`kakao_config.php`는 `static` 변수로 캐싱하므로, **같은 요청 내에서는** 설정이 바뀌지 않습니다.
하지만 새 요청(페이지 새로고침)에서는 DB에서 다시 로드합니다.

```bash
# PHP opcache가 문제라면 PHP-FPM 재시작
cd /home/cnst/www/html/webservice && docker compose restart php
```

### 문제: "재발송 횟수 제한 초과" 메시지

- `kakao_notifications` 테이블에서 해당 게시글의 재발송 기록 수 확인
- `parent_notification_id IS NOT NULL`인 레코드 수가 `KAKAO_RETRY_COUNT` 이상이면 차단

```bash
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT COUNT(*) as resend_count
      FROM kakao_notifications
      WHERE board_type='consignment' AND board_id=6918
      AND parent_notification_id IS NOT NULL;"
```

### 문제: "일일 발송 한도 초과" 메시지

```bash
# 오늘 발송 건수 확인 (failed 제외)
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT COUNT(*) FROM kakao_notifications
      WHERE DATE(created_at) = CURDATE() AND status != 'failed';"

# 현재 일일 한도 확인
docker exec project1_mysql mysql -u root -p'253X2vQ6iO7RfSqIH0iL' project1_db \
  -e "SELECT setting_value FROM site_settings WHERE setting_key='kakao_daily_limit';"
```

---

## 테스트 체크리스트

| # | 테스트 항목 | 결과 |
|---|------------|------|
| A1 | 승인 시 알림톡 발송 기록 생성 | [ ] |
| A2 | sent_by에 'admin' 저장됨 | [ ] |
| A3 | 미리보기 정상 동작 | [ ] |
| B1 | 거절 시 알림톡 발송 기록 생성 | [ ] |
| B2 | CONSIGN_REJECTED 템플릿 사용 | [ ] |
| C1 | 재발송 시 sent_by가 NULL이 아님 (버그 수정 확인) | [ ] |
| C2 | 재발송 시 parent_notification_id 설정됨 | [ ] |
| C3 | 횟수 제한 초과 시 에러 메시지 | [ ] |
| D1 | 일일 한도 초과 시 발송 차단 | [ ] |
| E1 | 발송 이력 정상 로드 (POST+CSRF) | [ ] |
| E2 | CSRF 없는 요청 → 403 | [ ] |
| E3 | GET 요청 → 405 | [ ] |
